import { withMixin } from "../../../helpers/dva";

const init = {};
export default withMixin({
  namespace: "XuzhenS",
  state: {
    ...init
  },
  subscriptions: {},
  effects: {},
  reducers: {}
});
